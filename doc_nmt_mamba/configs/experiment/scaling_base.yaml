# @package _global_
# Scaling Experiment: Base Model (77M params)
# Paper Reference: Figure 3
#
# Tests scaling behavior of Align-Mamba architecture.
# 12 encoder + 12 decoder layers, d_model=512

defaults:
  - /experiment/_base
  - override /model: hybrid_base
  - override /data: iwslt14_de_en
  - override /training: default

experiment:
  name: scaling_base
  paper_ref: "Figure 3 - 77M"
  description: "Scaling experiment: 77M parameter model"

project:
  name: align_mamba_icml
  seed: 42

logging:
  wandb:
    enabled: true
    tags: ["scaling", "figure3", "base", "77M"]
